window.onload = function() {
    main()
}

function main() {
    let config = {
        width: 800,
        height: 600,
        backgroundColor: 0x000000
    }
    let game = new Phaser.Game(config)
}